下面是你的第四阶段需求文档（V4.0 交互完善 + 内容闭环）。这一版重点解决三个核心问题：

图片上传（从“URL输入” → “本地上传”）

首页滚动交互（强制全屏切换，无残留）

评论系统（含评论点赞）



---

一、版本目标（V4.0）

将博客升级为完整内容平台雏形：

1. ✅ 支持图片本地上传（封面）


2. ✅ 首页交互从“普通滚动” → “沉浸式分屏切换”


3. ✅ 评论系统闭环（评论 + 点赞）




---

二、功能一：本地图片上传（封面图）


---

2.1 当前问题

现在：

只能填 URL ❌

用户体验差

不符合真实产品逻辑


目标：

直接上传图片

自动返回访问地址



---

2.2 技术方案（推荐）

方案选型（你当前阶段最优）

👉 本地存储（开发阶段） 👉 后续可升级 OSS（阿里云）


---

2.3 后端实现（Spring Boot）

上传接口

POST /api/upload
Content-Type: multipart/form-data


---

Controller

@PostMapping("/upload")
public String upload(@RequestParam("file") MultipartFile file) throws IOException {
    String fileName = System.currentTimeMillis() + "_" + file.getOriginalFilename();

    String path = "E:/自主博客/upload/";
    File dest = new File(path + fileName);
    file.transferTo(dest);

    return "/upload/" + fileName;
}


---

静态资源映射（必须）

@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        registry.addResourceHandler("/upload/**")
                .addResourceLocations("file:E:/自主博客/upload/");
    }
}


---

2.4 前端实现（Vue）

上传组件

<input type="file" @change="uploadImage" />


---

JS逻辑

async function uploadImage(e) {
  const file = e.target.files[0]

  let formData = new FormData()
  formData.append("file", file)

  const res = await axios.post("/api/upload", formData)

  blog.coverImage = res.data
}


---

2.5 UI优化（建议）

.upload-box {
  border: 2px dashed #FFB6C1;
  padding: 20px;
  text-align: center;
}


---

三、功能二：首页“强制分屏滚动”（核心优化）


---

3.1 你的目标（重新定义）

当前问题：

滚动是连续的 ❌

会出现“卡在中间”的状态 ❌


目标：

只存在两个状态：

欢迎页（100vh）

博客页（100vh）


滚动 = “切换页面”

不允许停在中间



---

3.2 技术方案（关键）

👉 监听滚轮 + 强制滚动


---

3.3 页面结构（必须这样）

<div class="container">
  <section class="hero"></section>
  <section class="main"></section>
</div>


---

3.4 核心逻辑（JS）

let currentPage = 0
let isScrolling = false

window.addEventListener("wheel", (e) => {
  if (isScrolling) return

  isScrolling = true

  if (e.deltaY > 0) {
    // 向下
    currentPage = 1
  } else {
    // 向上
    currentPage = 0
  }

  window.scrollTo({
    top: currentPage * window.innerHeight,
    behavior: "smooth"
  })

  setTimeout(() => {
    isScrolling = false
  }, 800)
})


---

3.5 CSS（必须锁死高度）

.hero, .main {
  height: 100vh;
}


---

3.6 关键优化（防止残留）

html {
  scroll-behavior: smooth;
  overflow: hidden; /* 禁止原生滚动 */
}


---

👉 这样效果就是：

轻轻一滑 → 直接切换页面

不存在“中间状态”



---

四、背景统一（你提到的割裂问题）


---

4.1 当前问题

欢迎页：粉白 ✔

博客页：深蓝 ❌



---

4.2 解决方案（统一背景变量）

:root {
  --bg-main: #F6F7FB; /* 月白色 */
}


---

.main {
  background: var(--bg-main);
}


---

👉 推荐颜色：

#F6F7FB  /* 月白 */
#F9FAFC  /* 更柔 */


---

五、功能三：评论系统（核心功能）


---

5.1 功能说明

支持：

发表评论

查看评论列表

评论点赞

点赞数显示



---

5.2 数据库设计

评论表

CREATE TABLE comment (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    blog_id BIGINT,
    user_id BIGINT,
    content TEXT,
    like_count INT DEFAULT 0,
    create_time DATETIME
);


---

5.3 点赞设计（评论）

Redis结构

key: comment:like:{commentId}
value: userId


---

5.4 接口设计


---

获取评论

GET /api/comment/list?blogId=1


---

发表评论

POST /api/comment/add

{
  "blogId": 1,
  "content": "写得很好"
}


---

评论点赞

POST /api/comment/like/{id}


---

六、前端评论区实现


---

6.1 结构

<div class="comment-box">
  <textarea v-model="content"></textarea>
  <button @click="submit">发布</button>

  <div v-for="c in comments" :key="c.id">
    <p>{{ c.content }}</p >
    
    <span @click="like(c)">
      ❤️ {{ c.likeCount }}
    </span>
  </div>
</div>


---

6.2 样式

.comment-box {
  margin-top: 30px;
}

.comment-item {
  background: var(--card-color);
  padding: 10px;
  border-radius: 10px;
  margin-bottom: 10px;
}


---

七、系统完整性（现在非常关键）

你现在系统已经具备：

模块	状态

用户系统	✅
博客系统	✅
点赞	✅
搜索	✅
UI主题	✅
评论系统	🔥（本次）



---

八、开发顺序（必须按这个）

Step 1

✅ 图片上传（最简单）


---

Step 2

🔥 强制滚动（最关键体验）


---

Step 3

✅ 背景统一


---

Step 4

🔥 评论系统（后端+前端）

