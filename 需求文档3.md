下面是你的**第三阶段增强需求文档（V3.0 UI/主题系统升级）**。这一版重点是：**视觉重构 + 主题切换 + 品牌化（Logo）**。所有内容都按“可直接落地实现”写。

---

# 一、版本目标（V3.0）

在 V2.0 基础上完成三件事：

1. **整体视觉升级**

   * 从「蓝色二次元」→「白底 + 粉色系 + 轻拟真樱花」
2. **主题系统**

   * 支持「亮色 / 暗色」一键切换（带图标）
3. **品牌标识**

   * 左上角 Logo 接入

---

# 二、视觉风格重构（核心）

## 2.1 设计目标

关键词：

* 白色背景
* 粉色主色调
* 柔和、干净
* 轻拟真（不是纯卡通）

---

## 2.2 新颜色体系（必须统一）

### 🌸 亮色主题（默认）

| 类型    | 颜色      |
| ----- | ------- |
| 主色（粉） | #FF8FB1 |
| 深粉    | #FF5C8A |
| 浅粉    | #FFD1DC |
| 背景    | #FFF6F8 |
| 卡片    | #FFFFFF |
| 边框    | #F3D1D9 |
| 字体主   | #333    |
| 次文字   | #888    |

---

### 🌙 暗色主题

| 类型 | 颜色      |
| -- | ------- |
| 主色 | #FF8FB1 |
| 背景 | #1A1A1A |
| 卡片 | #2A2A2A |
| 边框 | #444    |
| 字体 | #EEE    |

---

## 2.3 全局CSS变量（必须做）

```css
:root {
  --bg-color: #FFF6F8;
  --card-color: #FFFFFF;
  --primary-color: #FF8FB1;
  --text-color: #333;
}

[data-theme="dark"] {
  --bg-color: #1A1A1A;
  --card-color: #2A2A2A;
  --text-color: #EEE;
}
```

---

## 2.4 全局应用

```css
body {
  background: var(--bg-color);
  color: var(--text-color);
}
```

---

# 三、功能一：粉色樱花动画（升级版）

## 3.1 动画目标

相比之前：

* ❌ 蓝色点 → ✅ 粉色花瓣
* ❌ 圆点 → ✅ 椭圆 + 旋转 + 飘落轨迹

---

## 3.2 Canvas实现（拟真）

```js
class Sakura {
  constructor() {
    this.x = Math.random() * window.innerWidth;
    this.y = Math.random() * window.innerHeight;
    this.size = Math.random() * 10 + 5;
    this.speedY = Math.random() * 1 + 0.5;
    this.speedX = Math.random() * 0.5 - 0.25;
    this.rotation = Math.random() * 360;
  }

  update() {
    this.y += this.speedY;
    this.x += this.speedX;
    this.rotation += 1;

    if (this.y > window.innerHeight) {
      this.y = 0;
    }
  }

  draw(ctx) {
    ctx.save();
    ctx.translate(this.x, this.y);
    ctx.rotate(this.rotation * Math.PI / 180);

    ctx.fillStyle = "rgba(255,182,193,0.8)";
    ctx.beginPath();
    ctx.ellipse(0, 0, this.size, this.size / 2, 0, 0, Math.PI * 2);
    ctx.fill();

    ctx.restore();
  }
}
```

---

## 3.3 优化建议

* 数量：30~50
* 使用 `requestAnimationFrame`
* 降低 CPU 占用（避免卡顿）

---

# 四、功能二：主题切换（亮色 / 暗色）

## 4.1 功能说明

位置：**右上角**

效果：

* ☀️（亮） → 🌙（暗）
* 页面颜色平滑切换

---

## 4.2 UI结构

```html
<div class="theme-toggle" @click="toggleTheme">
  <span v-if="isDark">🌙</span>
  <span v-else>☀️</span>
</div>
```

---

## 4.3 Vue逻辑

```js
import { ref, onMounted } from 'vue'

const isDark = ref(false)

onMounted(() => {
  const theme = localStorage.getItem('theme')
  if (theme === 'dark') {
    document.documentElement.setAttribute('data-theme', 'dark')
    isDark.value = true
  }
})

function toggleTheme() {
  isDark.value = !isDark.value

  const theme = isDark.value ? 'dark' : 'light'
  document.documentElement.setAttribute('data-theme', theme)

  localStorage.setItem('theme', theme)
}
```

---

## 4.4 动画（高级感）

```css
body {
  transition: background 0.5s, color 0.5s;
}
```

---

# 五、功能三：Logo接入（品牌化）

---

## 5.1 资源位置（你提供的）

```
E:\自主博客\frontend\public\images\logo.png
```

---

## 5.2 前端引用路径

```html
<img src="/images/logo.png" class="logo" />
```

---

## 5.3 Header结构优化

```html
<div class="navbar">
  <div class="left">
    <img src="/images/logo.png" class="logo" />
    <span class="title">My Blog</span>
  </div>

  <div class="right">
    <!-- 主题切换 -->
    <!-- 登录注册 -->
  </div>
</div>
```

---

## 5.4 样式

```css
.logo {
  height: 40px;
  margin-right: 10px;
}

.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
```

---

# 六、卡片UI升级（重要）

## 6.1 当前问题

* 单调
* 层次不够

---

## 6.2 新样式

```css
.blog-card {
  background: var(--card-color);
  border-radius: 20px;
  padding: 20px;
  margin-bottom: 20px;

  box-shadow: 0 10px 30px rgba(255, 182, 193, 0.2);
  transition: transform 0.3s;
}

.blog-card:hover {
  transform: translateY(-5px);
}
```

---

# 七、背景优化（关键）

## 7.1 轻纹理背景（推荐）

```css
body {
  background: linear-gradient(180deg, #FFF6F8, #FFE4EC);
}
```

---

## 7.2 叠加你的动漫图（弱化）

```css
body::before {
  content: "";
  position: fixed;
  width: 100%;
  height: 100%;
  background: url('/images/bg.png');
  opacity: 0.1;
}
```

---

# 八、开发顺序（必须按这个来）

### Step 1（30分钟）

✅ 引入 CSS 变量（主题系统基础）

---

### Step 2（1小时）

✅ 实现主题切换（含 localStorage）

---

### Step 3（1小时）

✅ 替换颜色体系（全站）

---

### Step 4（1.5小时）

🔥 重写樱花动画（粉色版）

---

### Step 5（30分钟）

✅ 接入 Logo

---

### Step 6（1小时）

✨ 卡片UI优化

---
